# Host Contract

## 首发宿主

- GitHub Copilot
- Codex

## 宿主最小要求

1. 能暴露 `skills`
2. 能暴露共享 `templates` 与 `contracts`
3. 对当前正式支持宿主，必须暴露 `shitpm-commands`
4. 同时必须能直接调用短技能

## 最小桥接校验

- `pm-go` 可见
- `pm-prd` 可见
- `pm-rv` 可见
- `shitpm-commands` 可读
- `templates` 可读
- `contracts` 可读
