# V2.1 固定样例冒烟验证

## 样例信息

- `样例项目`：供应商准入申请管理（最小版）
- `执行日期`：2026-04-17 10:07:28 +08:00
- `执行人`：Codex
- `分支 / 提交`：`V2 / 4339dc1`

## 样例范围

- `角色`：申请人、审批人
- `核心闭环`：提交申请 -> 审批 -> 查看结果
- `页面数`：3-4 个
- `功能点数`：8-12 个
- `必含场景`：表单、列表、状态流转、异常提示

## 执行记录

### 基础链路

- [ ] `/start` 能正常初始化项目
- [ ] 自然语言输入触发 `scope`
- [ ] `/mind -> /feat -> /page -> /prd -> /rev` 跑完整链路
- [x] 门禁在 `page / prd / rev` 阶段正常生效

### 自审机制

- [ ] `sum` 生成后自动执行 5 项自审，不通过不进入状态更新
- [ ] `mind` 生成后自动执行 6 项自审，不通过不进入状态更新
- [ ] `feat` 生成后自动执行 7 项自审，不通过不进入状态更新
- [ ] `page` 生成后自动执行 7 项自审，不通过不进入状态更新
- [ ] `prd` 生成后执行一致性校验门禁，不通过不进入状态更新
- [ ] `prd` 生成后执行角色视角自审

### 防幻觉

- [ ] `PRD` 中不出现 `Feature List` 中没有的功能点
- [ ] `PRD` 中不出现 `Page Structure` 中没有的页面或区域
- [ ] 找不到依据的内容被标记为 `[待确认]`，而不是编造

### 回退机制

- [ ] 构造一个 `feat` 来源不清的能力点，验证会回退 `mind`
- [ ] 构造一个 `page` 承载缺口，验证会回退 `feat`
- [ ] 构造一个上游缺失场景，验证 `prd` 不会脑补，而是标记 `[待确认]` 或回退

### 方法论

- [ ] `scope` 阶段使用问题框架澄清
- [ ] `sum` 阶段使用资料分析框架
- [ ] `mind` 阶段遵循 `MECE` 原则
- [ ] `feat` 阶段使用简化 `RICE` 评估优先级
- [ ] `page` 阶段遵循信息架构原则

### 评审

- [ ] `rev` 阶段的一致性检查包含 9 项
- [ ] 评审结论能区分：通过 / 带条件通过 / 阻塞

## 每一步输入与输出

### Step 1

- `输入`：运行 `powershell -NoProfile -File scripts/smoke-test.ps1`
- `实际输出`：仓库结构、公开技能和命令映射检查通过；返回 `status=ok`
- `门禁结果`：通过
- `问题与修复`：无

### Step 2

- `输入`：运行 `powershell -NoProfile -File scripts/gate-regression.ps1`，场景 1
- `实际输出`：稳定 `feature_list` 基线存在时，`page` 门禁放行
- `门禁结果`：通过
- `问题与修复`：无

### Step 3

- `输入`：运行 `powershell -NoProfile -File scripts/gate-regression.ps1`，场景 2
- `实际输出`：写入 blocker 后，`prd` 门禁被阻塞
- `门禁结果`：通过
- `问题与修复`：无

### Step 4

- `输入`：运行 `powershell -NoProfile -File scripts/gate-regression.ps1`，场景 3
- `实际输出`：写入 pending confirmation 后，`mock` 门禁被阻塞
- `门禁结果`：通过
- `问题与修复`：无

### Step 5

- `输入`：运行 `powershell -NoProfile -File scripts/gate-regression.ps1`，场景 4
- `实际输出`：无效稳定基线写入失败，且未错误创建 `docs/project-status.json`
- `门禁结果`：通过
- `问题与修复`：脚本按预期输出错误信息，这是本场景成功条件，不需要修复

### Step 6

- `输入`：检查 `skills/sum|mind|scope|feat|page|prd` 中新增章节位置与文案
- `实际输出`：方法论、自审、门禁章节均已落到目标文件
- `门禁结果`：通过
- `问题与修复`：无

### Step 7

- `输入`：宿主侧人工链路验证，含 `/start`、自然语言路由、整链产物生成
- `实际输出`：当前未在 AI IDE 宿主内执行
- `门禁结果`：待验证
- `问题与修复`：需在真实宿主环境补跑，并回填本文件

## 当前结论

- 已完成自动化验证：结构检查、状态写入、`page / prd / mock` 门禁回归、无效基线失败保护
- 已完成文档落地校验：`sum / mind / scope / feat / page / prd` 新增方法论、自审和门禁章节均存在
- 未完成宿主人工验证：`/start`、自然语言触发 `scope`、`/mind -> /feat -> /page -> /prd -> /rev` 全链路产物生成与角色自审
