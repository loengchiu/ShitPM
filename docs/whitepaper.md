# 白皮书

ShitPM 当前采用：

- 全局安装
- 项目级初始化
- `AGENTS.md` 统一路由
- 短命令 + 自然语言并存

## 工作流

`scope -> sum -> mind -> feat -> page -> prd -> rev -> mock -> note`

## 设计目标

- 不再依赖 `/pm-go`
- 仅在已初始化项目中激活
- 关键阶段保留门禁与最小自检
- 用状态文件维持阶段、阻塞项和稳定锚点
