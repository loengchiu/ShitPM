# 迭代记录

> 用于记录 ShitPM 每一轮修改的时间、功能要点和影响范围。后续每次变更都在顶部追加。

## 2026-04-14 00:55

### pm-rv 门禁与状态模型收口

- 更新 `scripts/stage-gate.ps1`
- 修正 `pm-rv` 的 runtime gate，不再无条件要求 PRD 存在
- 改为按 `Mindmap / Feature List / Page Structure / PRD` 任一可评审对象放行，并校验对应文件真实存在

### 评审记录接入状态模型

- 更新 `contracts/workflow-state.md`
- 更新 `scripts/status-write.ps1`
- 更新 `skills/pm-rv/SKILL.md`
- 更新 `contracts/done-criteria.md`
- 在 `latest_artifacts` 中新增 `reviews`
- 明确评审记录写入 `docs/review-meetings/` 后，还要同步写入 `latest_artifacts.reviews`

## 2026-04-14 00:42

### pm-rv 增加渐进式问答机制

- 更新 `skills/pm-rv/SKILL.md`
- 更新 `templates/review-checklist.md`
- 更新 `contracts/done-criteria.md`
- 更新 `contracts/error-handling.md`
- 明确当评审问题过多时，复用 `pm-scope` 的渐进式收敛思路
- 增加问题归并、去重、阻塞度排序、问题地图和单轮问题上限规则

### pm-rv 与 pm-fix 联动

- 更新 `skills/pm-rv/SKILL.md`
- 更新 `templates/review-checklist.md`
- 更新 `contracts/done-criteria.md`
- 更新 `contracts/error-handling.md`
- 明确评审问答后的答案若已改变事实源，必须从 `pm-rv` 路由到 `pm-fix`
- 明确 `pm-rv` 负责发现和定性问题，`pm-fix` 负责正式处理变更

## 2026-04-14 00:28

### pm-rv 扩大到 mm / fl / ps / prd 四层评审

- 更新 `skills/pm-rv/SKILL.md`
- 将 `pm-rv` 从以 `Feature List / Page Structure / PRD` 为主的评审器，扩展为覆盖 `Mindmap / Feature List / Page Structure / PRD` 的分层评审入口
- 新增“分层质量判定”部分，分别说明四层产物各自要检查什么

### 补充评审团组合模板

- 更新 `skills/pm-rv/SKILL.md`
- 更新 `templates/review-checklist.md`
- 更新 `contracts/done-criteria.md`
- 增加 `需求边界评审团 / 页面承载评审团 / 可开发性评审团 / 上线风险评审团`
- 强调多角色评审是角色视角和组合模板，不做戏剧化人设

## 2026-04-14 00:12

### 统一“当前工作稿 PRD”判定口径

- 更新 `skills/pm-fix/SKILL.md`
- 将“项目目录扫描到的最新 PRD”收紧为“候选 PRD 中结合命名、版本信息、修改时间和当前上下文判定出的当前工作稿”
- 明确：若仍无法判清，不自动认定，停止推进并要求用户确认

### 补上 PRD 对思维导图的回写路径

- 更新 `skills/pm-fix/SKILL.md`
- 更新 `skills/pm-rv/SKILL.md`
- 更新 `templates/review-checklist.md`
- 更新 `templates/fix-record.md`
- 更新 `contracts/done-criteria.md`
- 明确：若 PRD 改动影响业务流程、判断节点、状态分支或系统结构，应回写 `pm-mm`
- 明确：能力边界变化时，除 `pm-fl` 外，必要时还可能继续影响 `pm-mm` 与 `pm-ps`

## 2026-04-13 23:58

### pm-fix 增加自动改动记录

- 更新 `skills/pm-fix/SKILL.md`
- 明确要求：若本轮自动改动了其他生成物，必须同步留下修正记录
- 新增 `templates/fix-record.md`，统一记录 PRD 与上游回写之间的影响和改动痕迹
- 同步更新 `contracts/done-criteria.md` 的 `pm-fix` 完成定义

### 状态字段与脚本支持同步

- 更新 `contracts/workflow-state.md`
- 在 `latest_artifacts` 中新增 `fix_records`
- 更新 `scripts/status-write.ps1`，支持 `latest_artifacts.fix_records` 追加写入
- 更新 `scripts/smoke-test.ps1`，将修正记录模板纳入自检

## 2026-04-13 23:45

### pm-rv 中文化问题定性并补齐评审模板

- 更新 `skills/pm-rv/SKILL.md`
- 将 `Tiger / Paper Tiger / Elephant` 统一改为 `真实风险 / 过度担忧 / 待观察项`
- 同步更新问答模式中的默认判定和路由规则，避免中英混用

### 评审模板与完成定义同步

- 更新 `templates/review-checklist.md`
- 补齐评审基础信息、版本与锚点关系、评审结论、问题定性与问答模式等检查项
- 同步更新 `contracts/done-criteria.md` 的 `pm-rv` 完成定义，使其包含问题定性要求

## 2026-04-13 23:18

### 修正 skill 内的绝对路径硬编码

- 更新 `skills/pm-analysis/SKILL.md`
- 更新 `skills/pm-mm/SKILL.md`
- 更新 `skills/pm-prd/SKILL.md`
- 将我误写进去的 `D:\work\ShitPM\...` 绝对路径统一改回相对路径引用
- 保持技能在不同工作机、不同宿主和不同仓库位置下都可复用，不再依赖本地固定目录

## 2026-04-13 23:05

### pm-prd 回归旧版强规则

- 更新 `skills/pm-prd/SKILL.md`
- 恢复旧版 PRD 的强规则密度，明确内容来源优先级、上游锚点约束、历史材料只能补充
- 恢复“业务属性继承”规则，要求角色、范围、优先级影响和页面承载关系不能在 PRD 中丢失
- 恢复详细需求说明的强约束，明确展示、操作、输入、加载、弹窗、异常、数据规范和边界场景的写法
- 恢复 Mermaid 图补充条件和复杂场景分块生成规则

### 模板与完成定义同步

- 更新 `templates/prd.md`
- 恢复更完整的 PRD 模板骨架，包括版本变更说明、业务诉求、流程与状态图、外部接口与依赖、数据字典、非功能要求、验收标准、附录与来源
- 同步更新 `contracts/done-criteria.md` 的 `pm-prd` 完成定义

## 2026-04-13 22:48

### 补回旧版 PS 的共同事实源规则

- 更新 `skills/pm-ps/SKILL.md`
- 明确 `Page Structure` 是 PRD 与 Prototype 的共同页面事实源
- 恢复“已有 PRD / Prototype 只能校验或补充参考，不能反向发明需求”的规则
- 新增 `pm-ps` 与下游 `pm-prd` / `pm-pt` 的显式衔接说明

## 2026-04-13 22:35

### pm-mm 与思维导图模板对齐

- 更新 `skills/pm-mm/SKILL.md`
- 明确思维导图阶段包含“导图初版 + 二次澄清回写 + 已收敛导图交付”三段逻辑
- 明确导图必须覆盖范围、主流程、模块、系统结构、关键判断点、状态分支、风险点和待澄清问题
- 明确二次访谈不是额外主产物，而是对导图的修订动作
- 新增“关键修订必须保留痕迹”的规则

### 模板与完成定义同步

- 更新 `templates/mindmap-spec.md`
- 新增一级分支：范围、模块、系统结构、修订记录
- 明确每类分支承载什么，不承载什么
- 同步更新 `contracts/done-criteria.md` 的 `pm-mm` 完成定义

## 2026-04-13 22:18

### pm-analysis 与项目简报模板对齐

- 更新 `skills/pm-analysis/SKILL.md`
- 明确项目简报必须包含“资料分析结论”“二次访谈补充与修正”“变更与修正留痕”
- 在技能规则中显式要求区分已确认事实、高可信推断和冲突/待核实信息
- 补充“二次访谈补充导致理解冲突时，先修订项目简报再继续”的回退逻辑
- 同步更新 `contracts/done-criteria.md` 的 `pm-analysis` 完成定义

## 2026-04-13 22:05

### 补强项目简报模板

- 更新 `templates/project-brief-lite.md`
- 在项目简报中补入“资料分析结论”章节，用于区分已确认事实、高可信推断和冲突信息
- 新增“二次访谈补充与修正”章节，用于承接补访谈后的新增信息、理解修正和仍待确认项
- 将“需求变更记录”扩展为“变更与修正留痕”，把需求变更、理解修正和访谈补充统一留痕

## 2026-04-13 21:40

### 恢复正式分析阶段

- 恢复 `pm-analysis` 为正式内部阶段，插入到 `pm-scope` 与 `pm-mm` 之间
- 明确 `pm-analysis` 的职责是沉淀分析简报，不再把“范围判断”直接跳到思维导图
- 补齐 `pm-analysis` 的完成定义、错误处理、交接关系和状态更新规则

### 主链与校验同步

- 主链更新为 `pm-go -> pm-scope -> pm-analysis -> pm-mm -> pm-fl -> pm-ps -> pm-prd -> pm-rv -> pm-pt -> pm-pa`
- `stage-gate.ps1` 新增 `pm-analysis` 目标并作为软阶段放行
- `smoke-test.ps1` 与 `verify-mappings.ps1` 纳入 `pm-analysis` 技能校验
- 重新校验 Codex 安装映射，确认 `pm-analysis` 已进入宿主技能目录

### 文档同步

- 更新 `contracts/stage-gates.md`
- 更新 `contracts/done-criteria.md`
- 更新 `contracts/error-handling.md`
- 更新 `docs/usage.md`
- 更新 `docs/real-project-test.md`
- 更新 `docs/whitepaper.md`
- 更新 `skills/pm-go/SKILL.md`，使其先判断是否缺分析产物，再决定进入思维导图

## 2026-04-13 20:50

### 恢复 Feature List 规则密度

- 对照旧版 PMsuperpowers 的 `pm-feature-list` 规则，补强当前 `pm-fl`
- 恢复输入事实源优先级、禁止项、B 端角色口径、关键需求摘要、完成定义、错误处理与交接规则
- 保留当前版较清晰的表达方式，但不再把 `pm-fl` 简化为“只会生成功能清单的提示词”

## 2026-04-13 19:40

### 自然语言主入口收口

- 明确普通用户默认只通过 `pm-go` 与系统交互
- `pm-go` 增加“真实状态判断 / 上下文推断”的边界约束，避免误导用户
- README、MANUAL、usage、copilot instructions 统一为“自然语言优先，但不伪装未实际发生的状态恢复 / gate / 落盘”

### 文档统一

- 收敛普通用户入口口径，弱化子命令在用户文档中的存在感
- 调整 `docs/real-project-test.md`，避免把内部子技能测试路径和普通用户主流程混在一起

## 2026-04-13 18:20

### 运行时门禁加固

- 新增 `status-read.ps1`、`status-write.ps1`、`stage-gate.ps1`
- 状态写入改为 patch 风格，不再要求模型手写整段状态 JSON
- `stage-gate.ps1` 对关键阶段执行运行时前置校验
- 强化 `stable_baselines` 与 `latest_artifacts` 的区分
- baseline 路径不存在时拒绝写入，避免把无效锚点落盘
- `blockers` 与 `pending_confirmations` 纳入门禁校验

## 维护规则

- 新迭代统一追加在文件顶部
- 每条记录至少包含：修改时间、功能要点、影响范围
- 如某次迭代包含脚本、技能和文档联动，建议拆成多个小节记录
