# 页面清单

**项目**：
**阶段**：Page Structure
**日期**：
**版本**：
**上游锚点**：

## 页面结构思维导图

### P-01 页面名称

**页面目标**：

```text
├── 页面名称
│   ├── 区域名称
│   │   ├── 对应功能点：
│   │   ├── 作用：
│   │   ├── 关键操作：
│   │   └── 关键状态：
```

## 规则

- 只保留页面目标与页面结构树
- 不输出页面总表
- 不输出结构稳定性结论
- 不输出使用说明和自检

## 说明

- `对应功能点` 只做映射，不重复解释功能
- `作用` 只说明区域职责
- `关键操作` 只写区域级主要动作
- `关键状态` 只写会影响后续 PRD 和原型的状态
